//1.2
class permution
{
    boolean per(String s,String t)
    {
        return sort(s).equals(t);
    }
    Static String sort(String s)
    {
        char[] s1=new char[s.length()]; 
        for(int i=0;i<=s.length();i++)
            for()
    }
    public Static void main(String[] args)
    {
        String s="abc";
        String t="cba";
        per(s,t);
    }
}